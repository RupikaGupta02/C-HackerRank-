#include <stdio.h>

int main() {

    int N;
    scanf("%d", &N);
    for(int i=1, count = 0; count < N;i++){
        if((i * i) % 3 != 0){
            printf("%d", i *i);
            count++;
            if(count < N)   
                printf(" ");
        }
    }
    return 0;
}
