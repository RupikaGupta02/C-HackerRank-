#include <stdio.h>

int main() {

int a,b;
    scanf("%d %d",&a,&b);
     int diff = a^b;
    
        if((diff & (diff-1))==0){
            printf("Yes");
        }
    else if(diff==0){
        printf("No");
    }
      else
          printf("No");

    return 0;
}
